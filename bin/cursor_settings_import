#!/usr/bin/env bash
set -euo pipefail

echo -e "\033[33mcursor_settings_import is deprecated; use: app_config restore cursor\033[0m" >&2

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec "${SCRIPT_DIR}/app_config" restore cursor "$@"
