#!/usr/bin/env bash
set -euo pipefail

echo -e "\033[33mcursor_settings_export is deprecated; use: app_config backup cursor\033[0m" >&2

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec "${SCRIPT_DIR}/app_config" backup cursor "$@"
