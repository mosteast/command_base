#!/usr/bin/env node
"use strict";

const { main } = require("../lib/git_init/cli.js");

main(process.argv.slice(2))
  .then((code) => {
    process.exit(code);
  })
  .catch((error) => {
    console.error(error.message || error);
    process.exit(1);
  });
